# Project-internal directories — never commit
documents/
workflow/
plan/
prompts/
feedbacks/

# Claude Code tooling — selective
# Skills are tracked (project tooling worth shipping with the repo).
# Personal config and AI-context docs stay local only.
.claude/*
!.claude/skills/
CLAUDE.md
memory/
PRODUCT.md
DESIGN.md

# Audit artifacts — generated by skills, kept local for review
motion-audits/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Node / build artifacts (in case the project grows in that direction)
node_modules/
dist/
build/
.env
.env.local

# Logs
*.log
npm-debug.log*

# Python bytecode (from skill scripts)
__pycache__/
*.pyc
